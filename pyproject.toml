[project]
name = "ruler-workspace"
version = "0.0.0"
description = "Ruler monorepo root (uv workspace)."
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["packages/python-engine", "apps/demo-api"]

[tool.uv.sources]
ruler-engine = { workspace = true }
